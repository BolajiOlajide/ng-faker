/**
 * namespace ngFaker.name
 */

function Name() {
    /**
     * firstName
     *
     *  @method firstName
     * @param {mixed} gender
     * @memberof faker.name
     */

    this.firstName = function (gender) {
        //
    };
}

module.exports = Name;
